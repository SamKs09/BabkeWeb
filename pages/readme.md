# Pages Directory

This directory is designated for secondary static HTML pages (e.g., policy pages, terms of service, detailed menu views, etc.).

Currently, all core site views are dynamically rendered in the main landing page (`index.html`) using the local storage mock database layer.
